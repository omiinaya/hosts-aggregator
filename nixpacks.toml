[phases.build]
cmds = [
  "npm install",
  "cd /app/backend && npm install",
  "cd /app/frontend && npm install",
  "cd /app && npm run build"
]

[start]
cmd = "npm run dev"

[variables]
NODE_ENV = "production"
NIXPACKS_PATH = ""
